#include "stdio.h"
#include "stdlib.h"
#define maxleng 100
typedef char SElemType;
typedef struct
{
    SElemType la[maxleng + 1];
    int length;
    int last;
} S

int


scanf ("%d", &e);
    while (!e)
    {
        PutElem (&L, e);
        scanf("%d", &e);
    }
    PritList (&L);
